{
  "name": "@playproof/api",
  "version": "0.1.0",
  "private": true,
  "description": "PlayProof API orchestrator (Fastify)",
  "type": "module",
  "scripts": {
    "dev": "node src/index.js",
    "build": "echo 'No build step required for API'"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  }
}
